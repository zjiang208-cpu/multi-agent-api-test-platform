# 安全基线

本文档描述当前版本已经实现的安全控制、默认边界和未覆盖的生产能力。
它适用于本地开发、隔离测试环境和作品演示，不代表平台已经具备公网生产系统所需的
身份认证、租户隔离、密钥托管或合规审计能力。

## 保护对象与安全原则

平台重点保护以下内容：

- LLM API Key、被测服务凭证、数据库 DSN、短信手机号和验证码；
- 业务需求文档、OpenAPI 契约、数据库结构和测试数据；
- 被测服务的真实执行目标以及带副作用的测试请求；
- HTTP 响应、Cookie、Token 和测试报告中的敏感字段。

当前版本遵循四条原则：

1. 凭证只保存引用，真实值只在需要执行时解析；
2. 默认拒绝未知远程目标和未经确认的真实执行；
3. AI 只负责受约束的理解与设计，执行和判定由确定性代码完成；
4. 所有默认限制都应在部署环境中继续由操作系统、网络和数据库权限兜底。

## 控制矩阵

| 安全区域 | 当前实现 | 明确边界 |
| --- | --- | --- |
| 凭证 | `env:NAME` 引用、内存解析、接口脱敏 | 不包含外部 Secret Manager 或密钥轮换服务 |
| 目标访问 | 默认仅允许 `localhost`、`127.0.0.1` 和 `::1` | 开启远程目标后由部署者负责网络隔离 |
| 执行授权 | 需求确认和执行确认两个 Human Gate | 当前没有平台用户身份与权限系统 |
| LLM 输入 | 已知敏感字段脱敏、长度限制、结构化输出校验 | 不是完整的 DLP 或内容分类系统 |
| 数据库证据 | 应用侧只读、显式表白名单、敏感列过滤 | 数据库账号本身仍必须使用真正的只读权限 |
| 持久化 | 项目元数据写入 `.data`，报告为进程内存存储 | 没有加密存储、备份和多实例一致性 |

## 1. 凭证与敏感信息

### 引用而不是明文

项目配置只接受类似下面的引用，不保存对应的真实值：

```text
env:DEEPSEEK_API_KEY
env:TEST_LOGIN_PASSWORD
env:TEST_DATABASE_DSN
```

LLM 凭证当前使用进程环境变量中的 `DEEPSEEK_API_KEY`。HTTP Auth Provider 的登录模板使用
`{{name}}` 占位符，并通过 `credential_refs` 指向环境变量。模型和 API 调用方不能通过未知字段
把密码或 Token 静默写入项目配置；项目模型会拒绝未声明字段和不符合引用格式的凭证字段。

### 运行时处理

- 登录请求只在执行期间把环境变量值替换进请求；
- 获取到的认证 Token 只保存在进程内存中，并按配置注入 Header 或 Cookie；
- 正常认证用例不会要求 Designer 把真实 Authorization、Cookie 或 Token 写入用例；
- 报告中的 `Authorization`、`Cookie`、`Set-Cookie`、`X-API-Key` 等响应头会被脱敏；
- 常见的 `password`、`token`、`secret`、`api_key` 等 JSON 字段会被替换为 `[REDACTED]`。

脱敏采用已知字段和关键词规则，不是完整的敏感信息识别系统。业务方仍应避免把不必要的
真实凭证写进需求原文、测试标题、URL 查询参数或自定义字段。

## 2. 出站请求与目标环境保护

### 默认策略

- `allow_remote_targets=false`；
- 目标必须是 HTTP(S) URL，且不能在 URL 中携带用户名或密码；
- 默认只允许本机目标；
- 默认不跟随重定向；即使开启重定向，也会对每一次出站请求重新检查目标策略；
- TLS 校验默认开启；
- Human Gate 会在真正发送请求前校验目标地址和执行范围。

远程目标只有在部署者明确开启配置后才可使用。开启后不等于安全，必须同时配置网络白名单、
隔离测试环境、最小权限账号和副作用数据清理策略。

### OpenAPI 与需求来源

远程 OpenAPI 来源默认关闭，本地文件和仓库内示例优先。上传的需求文档限制为 10 MB，解析后的
文本限制为 500,000 个字符；不支持的格式会被拒绝，YAML 使用安全加载方式，HTML 解析时跳过
`script`、`style`、`noscript` 和 `template` 内容。

## 3. LLM 数据边界

发送给 LLM 前，平台会对结构化映射和人工文本执行已知敏感字段脱敏，并限制提示词文本长度。
模型输出必须通过 JSON 解析和 Pydantic 结构校验，校验失败时只保留有限的错误信息；修复用的原始
payload 仅存在于内存中，不写入工作流元数据，也不通过 API 返回。

工作流还具备以下约束：

- 每次工作流有 LLM 调用预算，避免异常重试无限消耗；
- NLU、Designer、Reviewer 的输出只能进入对应的结构化阶段；
- Reviewer 不能直接执行 HTTP 请求；
- 生成的用例必须经过本地校验和人工确认，才能进入真实执行；
- 提示词以版本化 YAML 保存，工作流快照记录提示词版本和哈希，便于复现。

这不能替代模型服务商侧的数据保留和训练策略。若需求文档包含商业机密，应在发送前脱敏，
并确认所使用模型服务商的企业数据协议和区域要求。

## 4. 数据库证据与测试夹具

数据库证据功能只允许应用侧的只读路径：

- 配置模型将 `readonly` 固定为 `true`；
- 必须显式配置 `allowed_tables`，不允许任意读取数据库表；
- 敏感列不会被作为数据库夹具提供给模型；
- 模型只看到 `$DB_FIXTURE[...]` 占位符，真实标识值在本地解析后才用于请求；
- 数据库 DSN 仍只接受 `env:NAME` 引用。

应用侧只读检查不能替代数据库账号权限。部署时必须使用真正没有写权限的数据库账号，
并限制网络来源、数据库 schema 和允许访问的表。

## 5. 文件、输入与路径安全

- Pydantic 模型默认拒绝未知字段，并对名称、路径、列表数量和文本长度设置上限；
- 文档、OpenAPI 和 Operation YAML 会检查文件类型、大小和结构；
- 本地 YAML 工件写入前会解析并校验路径，防止逃逸出配置的数据目录；
- 平台不会导入或执行被测项目代码；
- 临时文件采用同目录临时写入后替换，避免留下半写入的配置文件；
- 请求响应体有长度上限，错误消息和可持久化诊断信息也会限制长度。

这些控制主要用于降低路径穿越、资源耗尽和配置污染风险，并不等价于完整的沙箱或任意文件
访问隔离。运行进程本身仍应使用低权限操作系统账号。

## 6. 本地存储与报告边界

项目、需求文档、接口目录和工作流工件保存在配置的数据目录，默认是 `.data/`。该目录可能包含
业务需求和测试元数据，不应提交到 Git，也不应放在公开静态目录下。

当前报告存储主要是进程内存：后端重启后报告会清空，多实例之间也不共享报告状态。因此当前版本
不提供以下生产能力：

- 平台用户登录、角色权限和租户隔离；
- 数据库级加密、密钥托管、备份恢复和保留周期；
- 完整不可抵赖的审计日志；
- 多实例部署下的统一队列和报告存储。

在没有补齐这些能力前，不要把开发服务、`/docs` 或 `/api` 直接暴露到公网，也不要在共享机器上
存放未脱敏的业务需求和凭证。

## 7. 发布前检查清单

提交代码或部署实例前，至少确认：

- [ ] `.env`、Token、密码、API Key、数据库连接串和真实需求文档没有进入 Git；
- [ ] `.data/`、`output/`、`tmp/` 和本地评测结果没有进入公开仓库；
- [ ] `allow_remote_targets` 和 `allow_remote_sources` 仍符合当前环境需要；
- [ ] 被测服务和数据库使用隔离环境及最小权限账号；
- [ ] TLS 校验没有被无理由关闭；
- [ ] LLM 提示词和需求文档不包含未经批准的个人信息或商业机密；
- [ ] 真实执行前已检查目标环境、鉴权配置和副作用用例；
- [ ] 运行目录和日志目录只有服务账号可读写；
- [ ] 依赖安装、后端测试和前端构建均已通过。

## 漏洞反馈

请不要在公开 Issue、README 或聊天记录中粘贴 Token、密码、数据库连接串或完整业务数据。
发现安全问题时，优先通过仓库的私密渠道联系维护者，并提供：影响范围、复现条件、最小复现步骤、
可能的修复建议以及是否已经暴露真实凭证。若凭证已经泄露，应先立即撤销或轮换，再进行问题说明。
